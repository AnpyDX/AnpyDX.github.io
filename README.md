# ad.BLOG 使用手册

文章 **MDX** 可用语法请参考 `samples` 目录下的官方示例。

## 可用 CLI

- 启动开发服务器：`npm run dev`
- 生成生产版本文件：`npm run build`
- 预览生产版本站点：`npm run preview`

## 部署至 GitHub Pages

在完成站点构建后，可在仓库根目录执行 `python deploy.py` 以完成自动部署。
